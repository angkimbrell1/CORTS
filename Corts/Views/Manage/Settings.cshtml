@using Corts.Models
@model SettingsViewModel

@{
    ViewBag.Title = "Settings";
}

<h2>Settings</h2>
<h2>Welcome @ViewBag.user</h2>
<hr>
<div id="vehicles">
    <div class="menu" id="left">
        @using (Html.BeginForm())
        {
            <section id="addVehicle">
                @Html.AntiForgeryToken()
                <h3>Add Vehicle</h3>
                @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                <div class="form-group">
                    <div class="col-md-10">
                        @Html.DropDownListFor(m => m.Car, new SelectList(Enum.GetValues(typeof(Car))), "Select Car")
                    </div>
                </div>
                <div class="form-group">
                    <div class="col-md-offset-2 col-md-10">
                        <input type="submit" value="Add" class="btn btn-default" />
                    </div>
                </div>
            </section>
        }

    </div>
    <div class="menu" id="right">
        @using (Html.BeginForm())
        {
        <section id="removeVehicle">
            @Html.AntiForgeryToken()
            <h3>Remove Vehicle</h3>
            @Html.ValidationSummary(true, "", new { @class = "text-danger" })
            <div class="form-group">
                <div class="col-md-10">
                    @Html.DropDownListFor(m => m.Car, new SelectList(Enum.GetValues(typeof(Car))), "Select Car")
                </div>
            </div>
            <div class="form-group">
                <div class="col-md-offset-2 col-md-10">
                    <input type="submit" value="Remove" class="btn btn-default" />
                </div>
            </div>
        </section>
        }
    </div>
</div>
<div id="prof">
    <div class="row" >
        <div class="col-md-8">
            <section id="profileSection">
                @using (Html.BeginForm("Settings", "Manage", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
                {
                    @Html.AntiForgeryToken()
                    <h3>Profile</h3>
                    <hr />
                    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                    <div class="form-group">
                        @Html.LabelFor(m => m.Username, new { @class = "col-md-2 control-label" })
                        <div class="col-md-10">
                            @Html.TextBoxFor(m => m.Username, new { @class = "form-control" })
                            @Html.ValidationMessageFor(m => m.Username, "", new { @class = "text-danger" })
                        </div>
                    </div>
                    <div class="form-group">
                        @Html.LabelFor(m => m.Email, new { @class = "col-md-2 control-label" })
                        <div class="col-md-10">
                            @Html.TextBoxFor(m => m.Email, new { @class = "form-control" })
                            @Html.ValidationMessageFor(m => m.Email, "", new { @class = "text-danger" })
                        </div>
                    </div>
                    <div class="form-group">
                        @Html.LabelFor(m => m.Password, new { @class = "col-md-2 control-label" })
                        <div class="col-md-10">
                            @Html.PasswordFor(m => m.Password, new { @class = "form-control" })
                            @Html.ValidationMessageFor(m => m.Password, "", new { @class = "text-danger" })
                        </div>
                    </div>
                    <div class="form-group">
                        <div class="col-md-offset-2 col-md-10">
                            <input type="submit" value="Submit" class="btn btn-default" />
                        </div>
                    </div>
                }
            </section>
        </div>
    </div>
</div>
@*<div id="prof">
    <h3>Profile</h3>
    <div class="data">
        <h4>Username: </h4><label>default_1</label>
    </div>
    <div class="changes">
        <button>Change Username</button><button>Change Password</button>
    </div>
    <div class="data">
        <h4>Email: </h4><label>default1@@aol.com</label>
    </div>
    <div class="changes">
        <button>Change Email</button>
    </div>
</div>*@
<div id="notifications">
    <div class="row">
        <div class="col-md-8">
            <section id="NotificationSection">
                @using (Html.BeginForm("Settings", "Notifications", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
                {
                    @Html.AntiForgeryToken()
                    <h3>Notifications</h3>
                    <hr />
                    <h4>How often would you like to recieve notifications?</h4>
                    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                    <div class="form-group">
                        <div class="col-md-10">
                            @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                            @Html.DropDownListFor(m => m.Car, new SelectList(Enum.GetValues(typeof(Notifications))), "Notification Options")
                        </div>
                    </div>
                    <h4>How often would you like to recieve mileage notifications?</h4>
                    <div class="form-group">
                        <div class="col-md-10">
                            @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                            @Html.DropDownListFor(m => m.Car, new SelectList(Enum.GetValues(typeof(Notifications))), "Notification Options")
                        </div>
                    </div>
                    <div class="form-group">
                        <div class="col-md-offset-2 col-md-10">
                            <input type="submit" value="Submit" class="btn btn-default" />
                        </div>
                    </div>

                }
            </section>
        </div>
    </div>
</div>
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}